{
  "name": "coffee-scroll-story",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "frames": "node scripts/generate-manifest.mjs",
    "predev": "npm run frames",
    "dev": "vite",
    "prebuild": "npm run frames",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
